import { useState } from 'react'
import { ArrowRight, Bot, ChevronRight, FileText, GraduationCap, HeartPulse, Landmark, Languages, Lightbulb, Menu, MessageCircle, Send, ShieldCheck, Sparkles, UserRound, X } from 'lucide-react'

const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000'
const categories = [
  { title: 'Identity & Documents', copy: 'Aadhaar, PAN, passports and more', icon: FileText, color: 'blue' },
  { title: 'Government Schemes', copy: 'Benefits and public services', icon: Landmark, color: 'violet' },
  { title: 'Health & Wellness', copy: 'Care made easier to understand', icon: HeartPulse, color: 'rose' },
  { title: 'Education & Careers', copy: 'Scholarships and opportunities', icon: GraduationCap, color: 'amber' },
]
const suggestions = ['How do I apply for an Aadhaar card?', 'What documents are needed for a passport?', 'Am I eligible for PM Kisan?', 'Tell me about scholarships for students']

function Brand() {
  return <a href="#top" className="flex items-center gap-2.5 text-base font-bold text-slate-950"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white shadow-lg shadow-blue-600/20"><Languages size={19} /></span>BharatSetu <span className="text-blue-600">AI</span></a>
}

function Navbar({ onChat }) {
  const [open, setOpen] = useState(false)
  return <nav className="sticky top-0 z-30 border-b border-slate-200/80 bg-white/85 backdrop-blur-xl"><div className="mx-auto flex h-[72px] max-w-7xl items-center justify-between px-5 sm:px-8"><Brand /><div className="hidden items-center gap-8 text-sm font-medium text-slate-500 md:flex"><a className="text-blue-600" href="#top">Home</a><a className="hover:text-slate-900" href="#categories">Explore services</a><a className="hover:text-slate-900" href="#about">About us</a></div><button onClick={onChat} className="hidden items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700 sm:flex">Ask BharatSetu <ArrowRight size={16} /></button><button aria-label="Toggle menu" onClick={() => setOpen(!open)} className="rounded-lg p-2 text-slate-600 md:hidden">{open ? <X size={22} /> : <Menu size={22} />}</button></div>{open && <div className="border-t border-slate-100 bg-white px-5 py-4 md:hidden"><div className="flex flex-col gap-4 text-sm font-medium text-slate-600"><a href="#top" onClick={() => setOpen(false)}>Home</a><a href="#categories" onClick={() => setOpen(false)}>Explore services</a><a href="#about" onClick={() => setOpen(false)}>About us</a><button onClick={() => { setOpen(false); onChat() }} className="flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-3 font-semibold text-white">Ask BharatSetu <ArrowRight size={16} /></button></div></div>}</nav>
}

function HomePage({ onChat, onSuggestion }) {
  return <><section className="relative overflow-hidden bg-white" id="top"><div className="hero-grid absolute inset-0 opacity-60" /><div className="absolute -right-24 top-20 h-72 w-72 rounded-full bg-blue-100/60 blur-3xl" /><div className="relative mx-auto grid max-w-7xl gap-16 px-5 pb-20 pt-16 sm:px-8 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:pb-28 lg:pt-24"><div><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-blue-100 bg-blue-50 px-3.5 py-2 text-xs font-bold uppercase tracking-[.15em] text-blue-700"><Sparkles size={14} /> Your guide to public services</div><h1 className="max-w-3xl text-5xl font-bold leading-[1.08] tracking-tight text-slate-950 sm:text-6xl lg:text-[72px]">The bridge to a <span className="text-blue-600">better tomorrow.</span></h1><p className="mt-7 max-w-xl text-base leading-7 text-slate-500 sm:text-lg">Understand government services, discover opportunities, and get things done - simply, clearly, and in your language.</p><button onClick={onChat} className="mt-9 inline-flex items-center gap-3 rounded-xl bg-blue-600 px-5 py-3.5 text-sm font-bold text-white shadow-xl shadow-blue-600/20 transition hover:-translate-y-0.5 hover:bg-blue-700">Start a conversation <ArrowRight size={18} /></button><div className="mt-9 flex items-center gap-3 text-sm text-slate-400"><span className="flex -space-x-2">{['bg-blue-200 text-blue-700', 'bg-indigo-200 text-indigo-700', 'bg-sky-200 text-sky-700'].map((style) => <span key={style} className={`grid h-7 w-7 place-items-center rounded-full border-2 border-white ${style}`}><UserRound size={13} /></span>)}</span> Made for every Indian</div></div><div className="relative mx-auto w-full max-w-md"><div className="absolute -inset-5 rounded-[2rem] bg-gradient-to-br from-blue-100 to-indigo-50 blur-2xl" /><div className="relative rounded-3xl border border-slate-200 bg-white p-5 shadow-2xl shadow-blue-900/10"><div className="flex items-center gap-3 border-b border-slate-100 pb-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-blue-600 text-white"><Bot size={21} /></div><div><p className="text-sm font-bold text-slate-900">BharatSetu Assistant</p><p className="flex items-center gap-1 text-xs text-emerald-600"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500" /> Online now</p></div><span className="ml-auto rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-bold text-blue-600">AI POWERED</span></div><div className="space-y-5 py-6"><div className="max-w-[85%] rounded-2xl rounded-tl-sm bg-slate-100 p-4 text-sm leading-6 text-slate-600">Namaste! I can help you navigate government services and schemes.</div><div className="ml-auto max-w-[82%] rounded-2xl rounded-tr-sm bg-blue-600 p-4 text-sm leading-6 text-white">How can I apply for an Aadhaar card?</div><div className="flex items-center gap-2 text-xs text-slate-400"><span className="grid h-6 w-6 place-items-center rounded-lg bg-blue-50 text-blue-600"><Bot size={13} /></span> BharatSetu is thinking...</div></div><button onClick={onChat} className="flex w-full items-center justify-between rounded-xl border border-blue-100 bg-blue-50 px-4 py-3 text-xs font-bold text-blue-700 hover:bg-blue-100">Try it yourself <ChevronRight size={15} /></button></div></div></div></section><section className="bg-slate-50/70 py-20" id="categories"><div className="mx-auto max-w-7xl px-5 sm:px-8"><div className="mb-9 flex items-end justify-between"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.16em] text-blue-600">Explore</p><h2 className="text-3xl font-bold tracking-tight text-slate-950">How can we help?</h2></div><button onClick={onChat} className="hidden items-center gap-1 text-sm font-bold text-blue-600 sm:flex">View all <ArrowRight size={16} /></button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categories.map(({ title, copy, icon: Icon, color }) => <button key={title} onClick={onChat} className="group rounded-2xl border border-slate-200 bg-white p-5 text-left transition hover:-translate-y-1 hover:border-blue-200 hover:shadow-xl hover:shadow-blue-900/5"><span className={`icon-${color} mb-10 grid h-11 w-11 place-items-center rounded-xl`}><Icon size={21} /></span><h3 className="font-bold text-slate-900">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{copy}</p><span className="mt-5 inline-flex items-center gap-1 text-xs font-bold text-blue-600 opacity-0 transition group-hover:opacity-100">Ask about it <ArrowRight size={13} /></span></button>)}</div></div></section><section className="bg-white py-20" id="about"><div className="mx-auto grid max-w-7xl gap-10 px-5 sm:px-8 lg:grid-cols-2 lg:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.16em] text-blue-600">Start with a question</p><h2 className="text-3xl font-bold tracking-tight text-slate-950">No jargon. No confusion.</h2><p className="mt-4 max-w-lg leading-7 text-slate-500">Ask in English, Hindi, or the language you are most comfortable with. BharatSetu finds the right path forward.</p></div><div className="grid gap-3 sm:grid-cols-2">{suggestions.map((question) => <button key={question} onClick={() => onSuggestion(question)} className="flex items-center justify-between rounded-xl border border-slate-200 px-4 py-3 text-left text-sm font-medium text-slate-600 hover:border-blue-200 hover:bg-blue-50 hover:text-blue-700"><span>{question}</span><ChevronRight className="ml-3 shrink-0" size={16} /></button>)}</div></div></section></>
}

function ChatPage({ initialMessage }) {
  const [input, setInput] = useState(initialMessage)
  const [messages, setMessages] = useState([])
  const [loading, setLoading] = useState(false)
  const sendMessage = async (event) => { event.preventDefault(); const message = input.trim(); if (!message || loading) return; setInput(''); setMessages((items) => [...items, { role: 'user', text: message }]); setLoading(true); try { const response = await fetch(`${API_URL}/chat`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message }) }); const data = await response.json(); setMessages((items) => [...items, { role: 'assistant', text: data.reply || data.error?.message || 'Something went wrong.' }]); } catch { setMessages((items) => [...items, { role: 'assistant', text: 'I could not connect to BharatSetu. Please check that the backend is running.' }]); } finally { setLoading(false) } }
  return <div className="mx-auto flex min-h-[calc(100vh-73px)] max-w-7xl"><aside className="hidden w-64 shrink-0 border-r border-slate-200 px-5 py-7 lg:block"><div className="mb-8 flex items-center gap-2 text-sm font-bold text-slate-900"><MessageCircle className="text-blue-600" size={18} /> New conversation</div><p className="mb-3 text-[10px] font-bold uppercase tracking-[.15em] text-slate-400">Categories</p><div className="space-y-1">{categories.map(({ title, icon: Icon }) => <button key={title} onClick={() => setInput(`Tell me about ${title}`)} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm text-slate-500 hover:bg-blue-50 hover:text-blue-700"><Icon size={16} /> {title}</button>)}</div><div className="mt-10 rounded-2xl bg-blue-50 p-4"><Lightbulb className="mb-3 text-blue-600" size={18} /><p className="text-xs font-bold text-blue-900">Tip</p><p className="mt-1 text-xs leading-5 text-blue-700">Ask a specific question for a more helpful answer.</p></div></aside><section className="flex min-w-0 flex-1 flex-col px-5 py-8 sm:px-10"><div className="mx-auto flex w-full max-w-3xl flex-1 flex-col"><div className="mb-8"><div className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-blue-600"><ShieldCheck size={14} /> BharatSetu AI</div><h1 className="text-3xl font-bold tracking-tight text-slate-950">How can I help you today?</h1><p className="mt-2 text-sm text-slate-500">Information made simple, for everyone.</p></div><div className="flex-1 space-y-5">{messages.length === 0 && <div className="grid gap-3 sm:grid-cols-2">{suggestions.map((question) => <button key={question} onClick={() => setInput(question)} className="rounded-2xl border border-slate-200 bg-white p-4 text-left text-sm text-slate-600 hover:border-blue-300 hover:shadow-md"><span className="mb-3 grid h-8 w-8 place-items-center rounded-lg bg-blue-50 text-blue-600"><Sparkles size={15} /></span>{question}</button>)}</div>}{messages.map((message, index) => <div key={`${message.role}-${index}`} className={`flex gap-3 ${message.role === 'user' ? 'justify-end' : 'justify-start'}`}>{message.role === 'assistant' && <span className="mt-1 grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-blue-600 text-white"><Bot size={16} /></span>}<div className={`max-w-[85%] whitespace-pre-wrap rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === 'user' ? 'rounded-tr-sm bg-blue-600 text-white' : 'rounded-tl-sm border border-slate-200 bg-white text-slate-600 shadow-sm'}`}>{message.text}</div></div>)}{loading && <div className="flex items-center gap-3 text-sm text-slate-400"><span className="grid h-8 w-8 place-items-center rounded-xl bg-blue-50 text-blue-600"><Bot size={16} /></span>BharatSetu is thinking...</div>}</div><form onSubmit={sendMessage} className="mt-8 flex items-center gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-lg shadow-slate-900/5 focus-within:border-blue-400 focus-within:ring-4 focus-within:ring-blue-50"><input value={input} onChange={(event) => setInput(event.target.value)} placeholder="Ask anything about a government service..." className="min-w-0 flex-1 bg-transparent px-3 py-2 text-sm outline-none placeholder:text-slate-400" /><button aria-label="Send message" disabled={loading} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50"><Send size={17} /></button></form><p className="mt-3 text-center text-[11px] text-slate-400">BharatSetu AI can make mistakes. Always verify details on official websites.</p></div></section></div>
}

function App() {
  const [page, setPage] = useState('home')
  const [initialMessage, setInitialMessage] = useState('')
  const openChat = (message = '') => { setInitialMessage(message); setPage('chat'); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  return <main className="min-h-screen bg-white text-slate-900"><Navbar onChat={() => openChat()} />{page === 'home' ? <HomePage onChat={() => openChat()} onSuggestion={openChat} /> : <ChatPage initialMessage={initialMessage} />}</main>
}

export default App